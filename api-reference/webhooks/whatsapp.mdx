---
title: "WhatsApp Meta Webhook"
sidebarTitle: "WhatsApp Webhook"
description: "GET and POST /webhook/whatsapp handle the Meta Cloud API verification handshake and inbound WhatsApp messages that resume the supervisor run."
---

`/webhook/whatsapp` is the Meta WhatsApp Cloud API webhook. Meta calls this endpoint, not you. Point your Meta app's webhook URL here. A UI does not need this endpoint.

## Endpoints

```http
GET /webhook/whatsapp
POST /webhook/whatsapp
```

## Verification handshake

Meta sends a `GET` request when you register the webhook URL. The endpoint echoes `hub.challenge` when `hub.verify_token` matches `WHATSAPP_WEBHOOK_VERIFY_TOKEN`, and returns `403` otherwise.

## Authentication

Meta signs every `POST` with the `x-hub-signature-256` header, an HMAC-SHA256 hex digest of the raw request body keyed by `WHATSAPP_APP_SECRET`.

## Event handling

`POST` receives inbound messages and delivery statuses. Every event is persisted to the WhatsApp message store. Inbound text messages are then matched to a request/supplier pair through `channel_threads` on the sender's `waId` and resume the supervisor. When several requests share the same `waId`, the most recently created request wins.

## Responses

| Status | Body | Meaning |
| --- | --- | --- |
| 200 | `OK` | Event processed, including deliberately ignored events: statuses, unmapped `waId`, redelivery of an already-recorded message, or a non-text message. |
| 400 | `Malformed JSON` | The request body could not be parsed. |
| 401 | `Invalid signature` | The `x-hub-signature-256` header did not match. |
| 500 | (empty) | Persistence or agent dispatch failed. Meta redelivers with backoff, and the last-message-ID dedupe keeps redeliveries from double-resuming the run. |
| 503 | `{ "error": "WhatsApp channel is not configured" }` | The `WHATSAPP_*` variables are not set. |

## Failure behavior

Outbound send failures that happen after Meta accepts a message, notably a free-form text sent outside the 24-hour service window (Meta error `131047`), arrive as status events and are recorded on the message. They never surface to the agent.

## Related

- [Webhooks Guide](/guides/webhooks)
- [Channels Guide](/guides/channels)
